# 服务端口号
server.port=8080
# jdbc
spring.datasource.username=root
spring.datasource.password=chensi0204
spring.datasource.url=jdbc:mysql://localhost:3306/gmall?serverTimezone=GMT%2B8&useSSL=false
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# mybatis配置
mybatis.mapper-locations=classpath:mapper/*Mapper.xml
mybatis.configuration.map-underscore-to-camel-case=true

# 日志级别
logging.level.root=info 

# dubbo的配置

# dubbo中的服务名称
spring.dubbo.application=redisson-test
# dubbo的通讯协议名称()
spring.dubbo.protocol.name=dubbo
# zookeeper注册中心的地址
spring.dubbo.registry.address=192.168.213.146:2181
# zookeeper的通讯协议的名称
spring.dubbo.registry.protocol=zookeeper
# dubbo的服务的扫描路径
spring.dubbo.base-package=com.sizheng.gmall
# 设置超时时间
spring.dubbo.consumer.timeout=60000
# 设置是否检查服务存在
spring.dubbo.consumer.check=false

# Redis配置
# 链接地址
spring.redis.host=192.168.213.146
# redis端口号
spring.redis.port=6379
# redis数据库
spring.redis.database=0

